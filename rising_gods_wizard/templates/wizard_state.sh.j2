# Rising Gods Wizard — auto-generated state file
# version={{ WIZARD_VERSION }}
# Sourceable via: source .wizard_state.sh
#
# Diese Vorlage ist die Referenz für den bash-sourceable State-File-Inhalt.
# Der Python-Wizard füllt sie via state.render(ctx) (siehe templates/__init__.py).
# Jinja ist KEINE Laufzeit-Abhängigkeit — der Inhalt wird direkt generiert.

export WOW_DIR="{{ WOW_DIR }}"
export INSTALL_MODE="{{ INSTALL_MODE }}"
export NTSYNC_ENABLED="{{ NTSYNC_ENABLED }}"
export GPU_CLASS="{{ GPU_CLASS }}"
export COMMUNITY_TOOLS="{{ COMMUNITY_TOOLS }}"
export ADDONS="{{ ADDONS }}"
export FPS_PROFILE="{{ FPS_PROFILE }}"
export REALMLIST_HOST="{{ REALMLIST_HOST }}"
